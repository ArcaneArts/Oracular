name: arcane_models
description: Shared models package for arcane_app
version: 1.0.0
publish_to: 'none'
environment:
  sdk: ^3.10.0

dependencies:
  artifact: ^1.0.34
  crypto: ^3.0.7
  fire_crud: ^3.1.16
  toxic: ^1.3.9

dev_dependencies:
  artifact_gen: ^1.3.10
  build_runner: ^2.14.0
  fire_crud_gen: ^1.1.5
  lints: ^6.1.0

scripts:
  # Oracular Commands
  check_tools: oracular check all

  # Code Generation
  build_runner: dart run build_runner build --delete-conflicting-outputs
  build_runner_watch: dart run build_runner watch --delete-conflicting-outputs

  # Firebase Rules
  deploy_rules: cd .. && firebase use FIREBASE_PROJECT_ID && firebase deploy --only firestore && cd arcane_app
